#include "main.h"
#include <unistd.h>
/**
 * _putchar - write the character c to stdout
 *  The character to print c
 *
 *  return: -1 is returned, and error is set appropriately.
 */

int _purchar(c)
{
	Return(write(1, &c, 1));

}
