#include <stdio.h>
/**
  * Description: main - print out strings stdout.
  * Return: always 0 if success.
 */
int main(void)
{
puts("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}

