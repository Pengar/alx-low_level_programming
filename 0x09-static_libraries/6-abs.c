#include "main.h"
/**
 *_abs - absolute value
 *
 *@r: the value stored in x through the conditional
 *
 *main - printing the value of a number
 *
 *Return: The value will return 0
 */

int _abs(int r)

{
	if (r > 0)
	{
		return (r);
	}
	else
	{
		return (r * -1);
	}
}
